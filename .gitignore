# Caches
.DS_Store
__pycache__
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
build

# Node — installed from package-lock.json, never committed. The built bundle
# under visualization/static IS committed; its inputs are not.
node_modules/

# Packaging — regenerated by every editable install, so tracking it produced a
# diff on any change to pyproject.toml, SUMMARY.md (the readme) or the file list.
*.egg-info/

# Editor
.vscode

# Misc
TRASH
commit-notes.txt

# Data
data/

# Temporary
tmp/

