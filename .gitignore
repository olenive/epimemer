# Caches
.DS_Store
__pycache__
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
build

# Node — installed from package-lock.json, never committed.
node_modules/

# The visualization bundle is build output: `make build-frontend` writes it,
# and the release workflow builds it into the wheel. Its inputs under
# visualization/frontend/src are what is committed.
epimemer/visualization/static/

# Packaging — regenerated by every editable install, so tracking it produced a
# diff on any change to pyproject.toml, the readme or the file list.
*.egg-info/

# Editor
.vscode
*.swp

# Misc
TRASH
commit-notes.txt

# Data
data/

# Temporary
tmp/

